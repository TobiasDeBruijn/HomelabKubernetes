apiVersion: apps/v1
kind: Deployment
metadata:
  name: login-server
  namespace: twinsight
  labels:
    app.kubernetes.io/name: login-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: login-server
  template:
    metadata:
      labels: 
        app.kubernetes.io/name: login-server
    spec:
      automountServiceAccountToken: false
      containers:
      - name: login-server
        imagePullPolicy: Always
        image: docker-registry.k8s.array21.dev/twinsight-login-server:0.1.0
        ports:
        - containerPort: 8080
          name: api
        env:
        - name: MYSQL_HOST
          value: mariadb.intern.array21.dev
        - name: MYSQL_DATABASE
          value: twinsight-prod-login-server
        - name: MYSQL_USERNAME
          valueFrom:
            secretKeyRef:
              name: login-server
              key: mysql-username
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: login-server
              key: mysql-password
        - name: PASSWORD_PEPPER
          valueFrom:
            secretKeyRef:
              name: login-server
              key: password-pepper
        - name: EMAIL_SENDER
          value: "noreply@signin.array21.dev"
        - name: FRONTEND_HOST
          value: "https://content-manager.staging.twinsight.media"
---
apiVersion: v1
kind: Service
metadata:
  name: login-server
  namespace: twinsight
spec:
  selector:
    app.kubernetes.io/name: login-server
  ports:
  - protocol: TCP
    name: api
    port: 8080
    targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: login-server
  namespace: twinsight
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: "letsencrypt-twinsight-media"
spec:
  tls:
  - hosts:
    - login.api.twinsight.media
    secretName: login-api-twinsight-media
  rules:
  - host: login.api.twinsight.media
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: login-server
            port:
              number: 8080
