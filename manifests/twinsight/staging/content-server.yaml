apiVersion: apps/v1
kind: Deployment
metadata:
  name: content-server-staging
  namespace: twinsight
  labels:
    app.kubernetes.io/name: content-server-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: content-server-staging
  template:
    metadata:
      labels: 
        app.kubernetes.io/name: content-server-staging
    spec:
      automountServiceAccountToken: false
      containers:
      - name: content-server-staging
        imagePullPolicy: Always
        image: docker-registry.k8s.array21.dev/twinsight-content-server:latest
        securityContext:
          capabilities:
            drop:
            - CAP_NET_RAW
        ports:
        - containerPort: 8080
          name: api
        env:
        - name: MYSQL_HOST
          value: mariadb.intern.array21.dev
        - name: MYSQL_DATABASE
          valueFrom:
            secretKeyRef:
              name: content-server-staging
              key: mysql-database
        - name: MYSQL_USERNAME
          valueFrom:
            secretKeyRef:
              name: content-server-staging
              key: mysql-username
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: content-server-staging
              key: mysql-password
        - name: LOGIN_SERVER_HOST
          value: http://login-server-staging.twinsight.svc.cluster.local:8080
        - name: DISCORD_WEBHOOK
          valueFrom:
            secretKeyRef:
              name: content-server-staging
              key: discord-webhook
---
apiVersion: v1
kind: Service
metadata:
  name: content-server-staging
  namespace: twinsight
spec:
  selector:
    app.kubernetes.io/name: content-server-staging
  ports:
  - protocol: TCP
    name: api
    port: 8080
    targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: content-server-staging
  namespace: twinsight
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-body-size: 1024m
    cert-manager.io/cluster-issuer: "letsencrypt-twinsight-media"
spec:
  tls:
  - hosts:
    - content.staging.twinsight.media
    secretName: content-staging-twinsight-media
  rules:
  - host: content.staging.twinsight.media
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: content-server-staging
            port:
              number: 8080
