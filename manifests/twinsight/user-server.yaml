apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-server
  namespace: twinsight
  labels:
    app.kubernetes.io/name: user-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: user-server
  template:
    metadata:
      labels: 
        app.kubernetes.io/name: user-server
    spec:
      automountServiceAccountToken: false
      containers:
      - name: user-server
        imagePullPolicy: Always
        image: docker-registry.k8s.array21.dev/twinsight-user-server:latest
        securityContext:
          capabilities:
            drop:
            - CAP_NET_RAW
        ports:
        - containerPort: 8080
          name: api
        env:
        - name: MYSQL_HOST
          value: mariadb.intern.array21.dev
        - name: MYSQL_DATABASE
          valueFrom:
            secretKeyRef:
              name: user-server
              key: mysql-database
        - name: MYSQL_USERNAME
          valueFrom:
            secretKeyRef:
              name: user-server
              key: mysql-username
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: user-server
              key: mysql-password
        - name: LOGIN_SERVER_HOST
          value: http://login-server.twinsight.svc.cluster.local:8080
---
apiVersion: v1
kind: Service
metadata:
  name: user-server
  namespace: twinsight
spec:
  selector:
    app.kubernetes.io/name: user-server
  ports:
  - protocol: TCP
    name: api
    port: 8080
    targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: user-server
  namespace: twinsight
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: "letsencrypt-twinsight-media"
spec:
  tls:
  - hosts:
    - user.api.twinsight.media
    secretName: user-api-twinsight-media
  rules:
  - host: user.api.twinsight.media
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: user-server
            port:
              number: 8080
